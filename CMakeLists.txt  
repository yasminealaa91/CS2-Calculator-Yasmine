cmake_minimum_required(VERSION 3.10)

Project(Calculator)
set(SOURCE_FILES
calculator.cpp
test.cpp
)
add_executable(trial ${SOURCES_FILES})
cmake_minimum_required (VERSION 3.10)

project (calculator)

set (SOURCE_FILES
calculator.cpp
test.cpp
)

add_executable (trial ${SOURCE_FILES})
